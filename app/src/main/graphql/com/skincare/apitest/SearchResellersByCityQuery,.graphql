query SearchResellersByCity($query: String!) {
    searchResellersByCity(query: $query) {
        id
        shop_name
        profile_picture_url
        reseller_name
        whatsapp_number
        facebook
        instagram
        city
        latitude
        longitude
    }
}